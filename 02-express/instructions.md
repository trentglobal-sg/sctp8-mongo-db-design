1. Create a new folder and init a new Node application: `npm init -y`
2. A NodeJS application exists in a folder. It will have a `package.json` file
3. Install packages: `npm install express cors dotenv`
   * `express`: a framework for running web-based backend
   * `cors`
   * `dotenv`
  